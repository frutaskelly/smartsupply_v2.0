import { PageHeader } from "@/components/ui/PageHeader";

export function ComingSoon({ title }: { title: string }) {
  return <PageHeader title={title} subtitle="Esta sección estará disponible próximamente." />;
}
